# Depends on: atk cogl json-glib gobject-introspection gtk3 gtk-doc libgudev libinput libxkbcommon wayland

description="A GObject based library for creating fast, visually rich graphical user interfaces"
url="https://blogs.gnome.org/clutter"
packager="jayce <jayce.net@gmail.com>"
contributors="Tnut"

name=clutter
version=1.26.2
release=1

source=(http://ftp.gnome.org/pub/gnome/sources/$name/${version%.*}/$name-$version.tar.xz)

build() {
cd $name-$version
./configure --prefix=/usr \
	--sysconfdir=/etc 	\
	--enable-egl-backend
make
make DESTDIR=$PKG install
}
